using UnityEngine;

namespace JohnStairs.RCC.Character.Cam {
    public class CauseCameraLookUp : MonoBehaviour {
        /* Assign this script to a game object to potentially trigger the camera look up functionality
        Requires variable EnableCameraLookUp set to true and LookUpTrigger set to "Component" */
    }
}
